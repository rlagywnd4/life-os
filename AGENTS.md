# LifeOS Codex Operating Rules

이 파일은 지피티와 이야기한 LifeOS 운영 방식 중 Codex가 지켜야 할 내용만 정리한 것이다.

## 역할 분리

- 사용자는 방향 결정과 실제 사용을 담당한다.
- Codex는 구현과 기록을 담당한다.
- GPT는 개념 문서를 관리한다.

## 상위 기준 문서

- `docs/LIFEOS_PRD.md`는 LifeOS의 상위 제품 기획 문서다.
- Codex는 이 문서를 임의로 수정하지 말고 구현과 의사결정의 기준으로 사용한다.

## 구현 전 확인

Codex는 현재 LifeOS 저장소를 먼저 분석한다.

- 기존 기술 스택.
- 아키텍처.
- 인증 방식.
- 데이터 저장 방식.
- 코딩 스타일.
- 예외 처리.
- API 응답 구조.
- UI 디자인 시스템.

## 기능 구현 범위

- 한 번에 너무 많은 기능을 넣지 않는다.
- 사용자가 정한 이번 작업 범위만 구현한다.
- 사용자가 정한 제외 범위는 구현하지 않는다.
- 기존 기술 스택, 아키텍처, 인증 방식, 데이터 저장 방식, 코딩 스타일, 예외 처리, API 응답 구조, UI 디자인 시스템을 유지하면서 구현한다.
- 기존 기능을 깨뜨리지 않도록 테스트한다.

## 기능 구현 후 갱신할 문서

작업을 완료한 뒤 다음 문서를 반드시 갱신한다.

1. `docs/10_CHANGELOG.md`
2. `docs/11_IMPLEMENTATION_STATUS.md`
3. 중요한 구현 또는 제품 결정이 있다면 `docs/09_DECISION_LOG.md`
4. `docs/handoffs/{날짜}-{작업명}.md`

## Handoff 문서에 포함할 내용

handoff 문서에는 다음 내용을 포함한다.

- 작업 목표.
- 구현한 내용.
- 변경 파일.
- 데이터 구조 변경.
- API 변경.
- 주요 구현 결정과 이유.
- 기존 PRD와 달라진 점.
- 임시 구현과 기술 부채.
- 실제 사용 후 확인해야 할 사항.
- 남은 문제.
- 다음 작업 후보.

## PRD 충돌 처리

- 단순 코드 변경과 제품 요구사항 변경을 구분한다.
- PRD와 충돌하는 내용이 생기면 `docs/LIFEOS_PRD.md`를 임의로 수정하지 않는다.
- 충돌 내용을 handoff와 decision log에 기록한다.

## 완료 보고

완료 후 실행 방법과 테스트 결과를 설명한다.

## Git 컨벤션과 배포 흐름

- 사용자에게 보이는 기능, 인증·권한, 데이터 구조, 의존성·CI·배포 설정 변경은 최신 `main`에서 `codex/<type>-<short-description>` 브랜치를 만든다. 브랜치 이름은 영문 소문자와 하이픈만 사용한다.
- 문서 오탈자·포맷·내부 기록처럼 사용자가 직접 확인할 필요가 없는 작은 유지보수는 `main`에 직접 커밋할 수 있다. 그 외 변경은 기능 단위 PR로 분리한다.
- 커밋은 하나의 의도만 담고 `<type>: <English summary>` 형식으로 작성한다. 허용 type은 `feat`, `fix`, `docs`, `refactor`, `test`, `chore`, `ci`다.
- 기능 PR은 검증이 끝나면 Ready for review 상태로 게시한다. UI 또는 사용자 흐름 변경에는 사용자가 직접 확인할 Preview 또는 테스트 빌드와 확인 방법을 제공한다. 구현 중이거나 결정·검증이 남았을 때만 Draft PR을 사용한다.
- PR 제목과 본문은 한국어로 작성한다. 본문에는 작업 목표와 사용자 변화, 직접 확인 방법, 주요 변경, 테스트·빌드 결과, 데이터·배포 영향, 남은 제한 사항을 기록한다.
- 기능 PR은 타입 검사, 테스트, 프로덕션 빌드가 통과해야 한다. 문서 전용 PR은 내용·링크·형식 검증을 한다. `main` 변경을 반영했다면 병합 전 다시 검증한다.
- 사용자는 Preview 또는 테스트 빌드에서 기능 결과를 확인하고 병합을 요청한다. Codex는 요청 후 Squash merge를 수행하며, 최종 커밋은 `<type>: <English summary> (#<PR number>)` 형식을 사용한다.
- 원격 push는 이 저장소의 SSH 설정을 사용하고, PR 생성·조회·병합 등 GitHub API 작업은 `scripts/gh-lifeos`를 사용한다. 이 도구는 Mac 키체인의 저장소 전용 GitHub App 키로 짧은 수명의 토큰을 자동 발급한다.
- 병합한 `codex/*` 브랜치는 삭제하고, 다음 작업은 최신 `main`에서 시작한다. `main`에는 force push하지 않으며, 문제 발생 시 revert 또는 긴급 수정 커밋으로 복구한다.
- 관련 없는 사용자 변경은 스테이징하거나 커밋하지 않는다. 테스트·빌드·인증·권한 문제가 있으면 숨기거나 우회하지 않고 사용자에게 알린다.
